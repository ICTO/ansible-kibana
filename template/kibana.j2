server {
    listen 80 default_server;
    server_name icto81.ugent.be; # This is just an invalid value which will never trigger on a real hostname.
    access_log logs/default.access.log main;
 
    server_name_in_redirect off;
 
    root  /var/www/;
}